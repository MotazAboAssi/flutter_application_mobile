import 'package:flutter/material.dart';

// Delivery Color Palette
final List<Color> palette = const [
  Color(0xFFB31510), // error
  Color(0xffffcb00), // links
  Color(0xffff5900), // primary
  Color(0xffcccccc), // background
  Color(0xffe2552b),
];

final errorColor = palette[0];
final linkColor = palette[1];
final primaryColor = palette[2];
final backgroundColor = palette[3];
final primaryHeavy = palette[4];
final lightBackgroundColor = Color.fromARGB(127, 204, 204, 204);
final primaryTextColor = Colors.white;
